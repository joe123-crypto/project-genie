name: Main Verification

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

concurrency:
  group: main-verification-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality-gate:
    uses: ./.github/workflows/reusable-quality-gate.yml
